# Скопируйте этот файл в .env и заполните перед деплоем
# cp .env.example .env

# Секретный ключ JWT — обязательно смените на случайную строку!
JWT_SECRET=replace_with_a_long_random_string_min_32_chars
